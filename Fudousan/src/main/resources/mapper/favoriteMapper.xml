<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.real.fudousan.favorite.dao.FavoriteDAO">
	<select id="allFavorite" resultType="favorite" parameterType="int">
		select
			memberId
			,estate
			,creDate
		from
			favorite
		where
			memberId = #{memberId}
	</select>
	
	<select id="searchFavorite" resultType="favorite" parameterType="int">
		select
			memberId
			,estate
			,creDate
		from
			favorite
		where
			memberId = #{memberID} and estate = #{estateId}
	</select>
	
	
</mapper>
